let options = {
    animate: true,
    patternWidth: 100,
    patternHeight: 100,
    grainOpacity: 0.05,
    grainDensity: 1.5,
    grainWidth: 1,
    grainHeight: 1
  };

  // eslint-disable-next-line no-undef
  export default (element) => grained(element, options)
  