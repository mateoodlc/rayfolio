let options = {
    animate: true,
    patternWidth: 300,
    patternHeight: 300,
    grainOpacity: 0.05,
    grainDensity: 2,
    grainWidth: 2,
    grainHeight: 2
  };

  // eslint-disable-next-line no-undef
  export default (element) => grained(element, options)
  